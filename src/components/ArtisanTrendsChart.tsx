import React from 'react';

// Monthly sales chart removed as per Fix 2 requirement (D3 dependency removed)
export const ArtisanTrendsChart: React.FC<{ data?: any[]; language?: string }> = () => {
  return null;
};
